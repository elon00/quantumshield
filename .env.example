# Optional server-side AI integration
GEMINI_API_KEY=
# Optional model override. Use a model available to your Gemini account.
GEMINI_MODEL=
# Public application URL; do not put secrets here
APP_URL=http://localhost:3000
# Local server port (hosting providers may override PORT)
PORT=3000
